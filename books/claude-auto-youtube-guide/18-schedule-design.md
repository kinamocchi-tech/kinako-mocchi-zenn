---
title: "スケジュール設計 ── 24時間自動実行タイムテーブル"
---

# スケジュール設計 ── 24時間自動実行タイムテーブル

## hourly-routing.mdとは

本書のシステムでは、何時に何を実行するかを
`docs/rules/hourly-routing.md` というファイルで定義しています。

Scheduled Tasksが毎時起動するたびに、Claudeはこのファイルを参照し、
「現在の時刻に対応するタスク」を実行します。

## 本書の標準タイムテーブル

現行の運用は、単一アカウントが24時間フル稼働する毎時24スロット構成です（複数アカウントに分けていた運用を2026年6月に1本へ集約しました）。深夜0時から4時台にも、台本・音声・画像・動画の各パイプラインが在庫状況に応じて追加スロットで動きます。代表的なスロットは次のとおりです（2026年7月時点）。

| 時刻（JST） | 実行タスク | 内容 |
|------------|----------|------|
| 00:00〜04:59 | audio・image・video・scriptの追加スロット | 深夜帯に在庫がある工程のみ実行 |
| 06:00 | script-pipeline | 台本の自動生成 |
| 07:00（月曜） | 週次マーケティング管理 | バッファ確認・スケジュール更新・週次レポート |
| 07:00（火〜日） | retro-try-handler + project-sync | 改善タスクの実行・Issueの整理 |
| 08:00 | research-runner → theme-discovery → refinement | Phase 2リサーチの自律実行・ネタ候補の収集・昇格 |
| 09:00・13:00・21:00 | sns-organic-pipeline | きなこ・もっちーのオーガニックSNS投稿 |
| 09:00 | Shorts在庫チェック | コンテンツ不足時にshorts-pipelineを起動 |
| 10:00 | audio-pipeline | 音声の自動合成 |
| 11:00 | project-sync | Issueの整理・PR確認 |
| 12:00 | image-pipeline | 画像の自動生成 |
| 14:00 | video-pipeline | 動画の自動制作 |
| 15:00 | YPP進捗確認 | 収益化条件の進捗チェック |
| 19:00 | スケジュール更新・日次進捗報告 | publishAt設定と本日の作業結果をSlackに通知 |

さらに、どのスロットも担当タスクに入る前に共通のプリフライトチェックを通します。現在は15種類のリカバリー処理が並んでおり、PRレビューの復帰確認から速報レーンの中断復帰、コメントの即時監視、ベンダー公式リリースの検知まで、取りこぼしを毎時拾い直します。

## Claudeが時刻を判断する仕組み

:::message
**重要な技術詳細**

Claudeのクラウドコンテナは世界中のサーバーで動作するため、
タイムゾーンがJST（日本標準時）に設定されていない場合があります。

本書のシステムでは `session-start.sh` で `export TZ=Asia/Tokyo` を実行し、
すべてのコマンドがJST時刻で動作するよう強制しています。

これにより「07:00のタスクが22:00に実行される」といったズレを防いでいます。
（実際にこの問題が発生し、`docs/rules/lessons/pipeline.md` の L-026 として記録されています）
:::

## タイムテーブルのカスタマイズ

自分のチャンネルに合わせてタイムテーブルを変更できます。

変更方法:
1. `docs/rules/hourly-routing.md` を編集する
2. 変更をGitHubにコミット・プッシュする
3. 次のScheduled Tasks実行から新しいスケジュールで動作する

:::details 一般的なカスタマイズ例
**制作ペースを増やしたい場合**:
- 06:00のscript-pipelineを「未処理テーマが2件以上あれば実行」から「1件以上あれば実行」に変更

**特定の曜日だけ動かしたい場合**:
- hourly-routing.md の各スロットに曜日条件を追加する
  例: `| 06:00 | 月・水・金のみ | script-pipeline |`
:::

## project-syncの役割

`project-sync` は本書のシステムのメンテナンス担当です。

| 処理内容 | 頻度 |
|---------|------|
| 進行中のまま放置されたIssueをリセット | 毎日 07:00・11:00 |
| レビュー待ちのPRを検出し、L1レビュー未実施ならフレッシュな `/code-review` を実行（外部レビュアーへの催促はしない） | 全スロット（毎時） |
| 古いブランチをリストアップ | 月曜のみ |

これにより、誰も対応していないタスクが放置されることを防ぎます。

