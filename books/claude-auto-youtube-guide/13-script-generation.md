---
title: "Phase 3 台本生成 ── キャラクターが語る台本をAIが書く"
---

# Phase 3 台本生成 ── キャラクターが語る台本をAIが書く

## このフェーズでやること

Phase 3では、Phase 2のリサーチ結果をもとに **2体のキャラクターが掛け合う台本** を自動生成します。

実行タイミング: **毎日 06:00 JST**（自動実行）

## script-pipelineの働き

`script-pipeline` スキルは以下の処理を自動実行します。

1. **リサーチ確認**: `content/research/V{ID}_research.md` を読み込む
2. **キャラクター設定の確認**: `docs/character-sheet.md` を参照
3. **台本生成**: キャラクターの個性・口調・関係性に基づいて台本を生成
4. **品質チェック**: 尺・ファクトチェック・キャラクター一貫性を自動確認
5. **台本JSONの保存**: `content/scripts/V{ID}_script.json` として保存
6. **GitHub PR作成**: 台本をレビュー用PRとして提出

## 台本のフォーマット

生成される台本は **JSON形式** で保存されます。

```json
{
  "video_id": "V005",
  "title": "Claude Code 3.7 Adaptive Thinking 完全解説",
  "target_duration_min": 15,
  "sections": [
    {
      "section_id": "opening",
      "lines": [
        {
          "character": "mocchi",
          "text": "きなこ〜！さっきニュース見たんだけど、Claude Codeがまたアップデートされたんだって！",
          "fact_check_flags": []
        },
        {
          "character": "kinako",
          "text": "ええ、Adaptive Thinkingね。私も気になってたのよ。",
          "fact_check_flags": []
        }
      ]
    }
  ],
  "fact_check_flags": [
    {
      "location": "section: features, line: 3",
      "claim": "処理速度が2倍になった",
      "rank": "B",
      "source_url": "https://...",
      "note": "公式ブログに記載あり。ベンチマーク詳細要確認"
    }
  ]
}
```

## 品質ゲート：尺のチェック

台本の品質は複数の観点で自動チェックされます。

最重要チェック項目のひとつが **「尺（時間の長さ）」** です。

| 尺の基準 | 理由 |
|---------|------|
| 最低 8分 | YouTube ミッドロール広告の閾値（8分未満では収益機会が大きく減る） |
| 目標 15〜20分 | テック解説として情報密度を確保できる尺 |
| 上限 20分 | 超えると視聴離脱が急増 |

尺が不足している場合、Claudeは自動で追加コンテンツを生成します。

## ファクトチェックフラグ

台本内の数値・日付・人名などは **`fact_check_flags`** で明示されます。

フラグのランク:
- **ランクA**: 一次ソース（公式サイト・論文）で確認済み
- **ランクB**: 二次ソースで確認済み（信頼性は高い）
- **ランクC**: 未確認（要注意）

ランクCのフラグが多い場合、Claudeは追加リサーチを自動実行します。

## AIによる台本レビュー

台本が生成されると、**複数のAIレビュアーが並列でレビュー** します。

| レビュアー | チェック内容 |
|-----------|------------|
| ファクトチェッカー（サブエージェント） | 数値・日付・固有名詞の正確性 |
| キャラクターレビュアー（サブエージェント） | もっちー・きなこらしさの一貫性 |
| 構成レビュアー（サブエージェント） | 流れ・わかりやすさ・視聴者への価値 |
| Gemini Code Assist | PR全体のコードレビュー |
| GitHub Copilot | PR全体のコードレビュー |

指摘があれば自動修正、問題なければGitHubにPRが作成されます。

:::message
あなたはPRを確認するだけでOKです。
気になる点があればPRコメントで修正を依頼できます。
特に問題なければ自動でマージされ、次のフェーズに進みます。
:::

次の章では、台本から **音声が自動生成される仕組み（Phase 4A）** を解説します。
